# مدار — آکادمی و رصدخانه فضایی

وب‌اپ فارسیِ واکنش‌گرا برای آموزش مهندسی فضا و مرور پژوهشی پرتاب‌ها، مدارها، پرتابگرها و شکست‌ها.

## قابلیت‌های نسخه 0.7

- کالبدشکافی پرتاب در ۱۰ مرحله با تصاویر اعتبارسنجی‌شده، fallback محلی و توضیح عمیق فیزیک، توالی، سامانه، تله‌متری، معیار موفقیت و شکست
- اطلس مدار توسعه‌یافته با محیط، اغتشاش، مخابرات، دسترسی، نگهداری، دفع، Checklist و نقشه طبقه‌بندی مدارها
- کتابخانه دانش سه‌سطحی شامل ۷۵ موضوع: ۵ حوزه × ۳ سطح × ۵ درس، با پیش‌نیاز، رابطه، مثال و منبع
- جست‌وجوی یکپارچه فارسی/انگلیسی در دانش، مسیرها، مدارها، ماهواره‌ها، پرتابگرها، شکست‌ها و داده زنده
- نسخه نصب‌شونده PWA با Manifest، آیکون‌های Android/iOS، Service Worker، App Shell و صفحه آفلاین
- پیکربندی آماده Render شامل `render.yaml`، `Procfile`، Health check و راهنمای انتشار
- Fallback ایستا برای بانک پرتاب‌ها، شکست‌ها و NASA/ESA؛ در پیش‌نمایش بدون سرور به‌جای HTTP 404 آخرین داده معتبر نمایش داده می‌شود
- مرکز NASA/ESA بهینه‌شده با همگام‌سازی هر ۳ دقیقه، فقط ۸ کارت در بار اول، Load more و تصاویر سبک‌تر
- رندر تنبل مدل سه‌بعدی و مقایسه معماری؛ مدل فقط هنگام ورود ساخته و با requestAnimationFrame چرخانده می‌شود
- مدل سه‌بعدی CSS قابل چرخش/لمس/Zoom با انتخاب ۱۰ جزء ماهواره
- مقایسه هم‌زمان ۶ معماری مرجع با ۵ نمودار کمّی و ۸ معیار کیفی
- سوییچ سراسری فارسی/English با ذخیره انتخاب کاربر و ترجمه خودکار Cache‌شده برای محتوای قدیمی
- نمایش هم‌زمان نام فارسی و نام رسمی انگلیسی در تازه‌ترین پرتاب‌ها و پرونده هر مأموریت
- دانشنامه ۸ خانواده ماهواره با کاربرد، محموله، مدار، دلیل انتخاب مدار و نمونه واقعی
- کالبدشناسی تعاملی ماهواره با ۱۰ جزء قابل انتخاب و توضیح دوزبانه وابسته به مأموریت
- ابزار «ماموریت ← مدار» با ۸ سناریو و ماتریس مقایسه‌ای
- ۹ تصویر فنی جدید و دقیق برای انواع ماهواره و اجزای فضاپیما
- مسیر یادگیری ۱۲‌ماژولی با ۵۹ فصل اصلی، توضیح علمی، رابطه، مثال حل‌شده و ۳۶ پرسش مفهومی
- مطالعه‌گر تمام‌صفحه با فهرست فصل، واژه‌نامه دوزبانه و ذخیره پیشرفت
- فونت محلی Vazirmatn UI با مجوز OFL و بدون وابستگی نمایشی به CDN
- کالبدشکافی تعاملی پرتاب در ۱۰ مرحله همراه تصویر واقعی یا نمودار فنی اختصاصی
- اطلس LEO/SSO/MEO/GEO/GTO/HEO و محاسبه‌گر مدار
- فهرست زنده پرتاب‌ها و شکست‌ها، جدید به قدیم، با Launch Library 2 و تصویر مأموریت/اعتبار صاحب اثر
- Proxy محلی امن برای تصاویر میزبان‌های مجاز و کش یک‌ساعته تصویر
- نمونه زنده عناصر مداری OMM از CelesTrak
- دانشنامه و مقایسه ۶ پرتابگر مهم
- پرونده‌های شکست با زنجیره «رخداد ← علت نزدیک ← علت ریشه‌ای ← اقدام اصلاحی»، سطح قطعیت و منبع
- خلاصه‌ساز لینک YouTube بر پایه کپشن عمومی؛ خلاصه، ترجمه فارسی، فصل‌بندی و واژه‌نامه
- محاسبه‌گر انتقال هوهمن و معادله موشک تسیولکوفسکی
- فهرست منابع رسمی، دانشگاهی، فارسی و انگلیسی

## اجرا

نیازمند Python 3.10 یا جدیدتر:

```bash
cd madar
python -m pip install -r requirements.txt
python app.py
```

سپس `http://localhost:8000` را باز کنید. برای تغییر پورت:

```bash
PORT=9000 python app.py
```

## منابع داده

- Launch Library 2 (The Space Devs): رکوردها و وضعیت پرتاب
- CelesTrak GP/OMM: عناصر میانگین مداری
- GCAT (J. McDowell): مسیر توسعه تاریخچه بلندمدت و کاتالوگ اشیا
- NASA / ESA / JAXA / ISRO / FAA: آموزش و گزارش‌های رسمی

زمان واکشی داده زنده در رابط نمایش داده می‌شود. پاسخ‌های upstream برای پنج دقیقه cache و آخرین پاسخ معتبر روی دیسک نگه‌داری می‌شود تا محدودیت موقت API، رابط را خالی نکند.

## محدودیت‌های مهم

1. «شکست پرتاب»، «شکست بازیابی» و «حادثه آزمون زمینی» یکسان نیستند؛ رابط آن‌ها را جدا می‌کند.
2. علت پرونده‌های باز قطعی تلقی نمی‌شود. برای تصمیم فنی، گزارش نهایی مرجع رسمی لازم است.
3. محاسبه‌گرها مدل دو جسم/ایده‌آل‌اند و جای STK/GMAT/Orekit، تحلیل اغتشاشات یا طراحی trajectory را نمی‌گیرند.
4. دریافت کپشن YouTube به عمومی‌بودن ویدئو، وجود کپشن و محدودنبودن IP بستگی دارد. ترجمه رایگان از سرویس عمومی استفاده می‌کند و ممکن است موقتاً در دسترس نباشد.
5. ظرفیت پرتابگر تابع مدار، میل، پایگاه، نسخه وسیله و بازیابی است؛ اعداد کارت‌ها صرفاً مرجع مقایسه مقدماتی‌اند.

## ساختار

```text
madar/
├── app.py                 # وب‌سرور، Feedهای زنده، ترجمه، proxy تصویر و خلاصه‌ساز
├── render.yaml            # Blueprint آماده Render
├── vercel.json            # تنظیم مسیرهای نسخه ورسل
├── vercel_common.py       # ابزار مشترک تابع‌های بدون‌سرور
├── api/                   # هشت تابع پایتونی ورسل
├── Procfile               # دستور اجرای سرویس وب
├── DEPLOY_RENDER.md       # راهنمای استقرار و نصب روی گوشی
├── requirements.txt
├── README.md
├── scripts/
│   ├── generate_module_covers.py
│   ├── generate_stage_diagrams.py
│   └── generate_satellite_visuals.py
├── vendor/                    # youtube-transcript-api و وابستگی‌های اجرای آفلاین
└── public/
    ├── index.html
    ├── manifest.webmanifest # مشخصات نصب PWA
    ├── service-worker.js    # Cache، آفلاین و مدیریت نسخه
    ├── offline.html         # صفحه قطع اتصال
    ├── icons/               # آیکون‌های ۶۴، ۱۸۰، ۱۹۲ و ۵۱۲
    ├── styles.css
    ├── data.js              # نقشه راه و داده‌های پایه
    ├── launch-detail.js     # محتوای عمیق ۱۰ مرحله پرتاب
    ├── orbit-detail.js      # تحلیل کامل ۶ خانواده اصلی مدار
    ├── learning-content.js  # ۱۲ درس‌نامه و ۵۹ فصل
    ├── satellite-data.js    # انواع، اجزا و نگاشت ماموریت به مدار
    ├── knowledge-data.js    # ۷۵ موضوع در سه سطح و پنج حوزه
    ├── failure-causes.js    # زنجیره علت پرونده‌های بررسی‌شده
    ├── app.js               # رابط و محاسبه‌گرها
    ├── fonts/               # Vazirmatn + مجوز OFL
    └── images/              # تصاویر مرجع و نمودارهای فنی
```

## گام‌های بعدی پیشنهادی

- ورود pipeline کامل GCAT و جست‌وجوی همه رکوردهای تاریخی
- پایگاه داده PostgreSQL و job زمان‌بندی‌شده برای ثبت نسخه داده
- صفحه مستقل برای هر مأموریت، ماهواره، پرتابگر، موتور و پایگاه
- افزودن نمودار reliability با تفکیک خانواده/نسخه پرتابگر
- موتور خلاصه‌سازی مبتنی بر مدل زبانی با استناد زمانی به transcript
- حساب کاربری، آزمون، فلش‌کارت و برنامه مطالعه تطبیقی
- شبیه‌ساز SGP4، ground track و پنجره تماس ایستگاه زمینی

## نام و هدف

«مدار» در این نسخه یک MVP پژوهشی/آموزشی است. هدف آن جایگزینی منبع اصلی نیست؛ هدف، تبدیل داده و منابع پراکنده به یک مسیر منسجم، قابل جست‌وجو و قابل پیگیری است.
